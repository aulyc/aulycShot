# 更新日志

自 1.8.0 起，aulycShot 的中文更新内容记录在此文件中。更早版本请查看英文版
`CHANGELOG.md`

## [Unreleased]

### 改进

- 将编辑画布几何、编辑器布局和图片拼接窗口控件拆分为职责单一的协作组件，并补充专项回归测试
- 显式标注 AppKit 与 ScreenCaptureKit 协作组件的并发所有权，并为有意使用的 `@unchecked Sendable` 保留运行时队列前置断言

### 修复

- 录屏恢复后从样本时间戳中扣除暂停区间，避免导出视频出现冻结空档
- 在准备、写入帧、暂停恢复、停止和取消竞态中保持录屏写入器变更只发生于专用队列，且完成回调仅交付一次

## [1.8.8] - 2026-08-03

### 修复

- 在 URLSession 删除临时下载前保留发布溯源和 DMG 文件，恢复应用内校验与安装更新
- 记录不含敏感信息的更新失败阶段和稳定错误码，便于定位故障

## [1.8.7] - 2026-08-03

### 改进

- 按职责拆分录屏、编辑器、钉图窗口和设置内部实现，同时保持现有操作流程
- 本地开发构建改为直接启动，不再替换 `/Applications` 中的应用

### 修复

- 将录屏写入器准备、帧时间、暂停恢复修正、停止、取消和完成回调统一到同一串行队列
- 收敛截图与录屏入口互斥，同时保留截图快捷键在录屏期间停止录制的行为
- 长截图裁剪限制在已选区域内，不再显示全屏背景界面
- 替换已废弃的 macOS 截图、应用激活和 AVFoundation 加载 API，并清零严格并发告警

## [1.8.6] - 2026-08-02

### 新增

- 新增默认启用的“自动检查更新”设置，可关闭每日静默检查并保留手动检查更新

## [1.8.5] - 2026-08-01

### 改进

- 更新“关于”页致谢，加入 AI 时代、Codex、Claude 与 aulyc，并保留对社区、用户和开源生态的感谢

## [1.8.4] - 2026-08-01

### 改进

- 缩窄更新提示面板并减小标题字号，使提示更紧凑并贴近系统窗口比例
- 钉住图片的缩放按钮改用普通减号和加号，并缩短其与缩放比例之间的距离

### 修复

- 普通启动和更新后重启时仅恢复菜单栏常驻，不再自动打开设置页

## [1.8.3] - 2026-08-01

### 修复

- 更新提示改用专用非模态面板，不再显示系统内部的问号、复选框和空白按钮，并精简已是最新版本文案
- 正式发布溯源从项目声明读取 Release Profile 版本，并拒绝缺失或不一致的版本元数据

## [1.8.2] - 2026-07-31

### 修复

- 新发布的更新清单保留 Team ID 和最低 macOS 版本，恢复旧版应用的检查更新能力
- 更新结果与安装提示改为非阻塞窗口，提示显示时仍可使用截图快捷键

## [1.8.1] - 2026-07-31

### 改进

- 下载更新安装包前先校验发布溯源，并在替换前验证版本、构建号、提交、应用身份、架构、校验和及 Developer ID 签名

### 修复

- 智能选择优先识别窗口和较大的结构区域，忽略单个控件及其他叶子元素，减少误选过小区域

## [1.8.0] - 2026-07-29

### 新增

- 新增紧凑的图片拼接工具，提供固定的间距、边距、圆角、背景和颜色预设
- 新增公开 GitHub 与 Gitee 更新镜像，两个镜像使用完全一致的正式 DMG 清单，并强制校验 SHA-256

### 改进

- 在设置侧边栏显示截图和录屏权限状态，并提供手动设置指引
- 按 GitHub、Gitee 顺序检查内容一致的公开更新清单，要求正式 DMG 提供 SHA-256，并在替换前验证已安装版本身份
- 切换应用时保持截图选区界面可见，仅当 Escape 或右键事件由选区界面接收时才取消截图
- 完成截图后默认选中矩形工具，并将标注光标限制在当前选区内
- 无障碍识别结果返回期间保留当前智能选区层级，并让全屏悬停边框保持在可绘制区域内
- 简化快捷键设置，所有截图和执行快捷键改为按需启用，并支持一次重置全部已配置快捷键
- 简化标注控制，采用固定的形状和箭头样式、统一蓝色选中状态、紧凑滑块和按需展开的隐藏工具网格
- 截图复制或保存成功后立即显示 1 秒提示，不再使用淡入淡出动画

### 移除

- 移除独立的权限页签和拖拽授权面板
- 移除默认双击 Command 触发、单独的编辑器保存快捷键，以及每行快捷键恢复按钮
- 移除 Emoji 标注工具及最近使用 Emoji 选择器
- 移除窗口截图阴影及相关设置，同时保留窗口轮廓和圆角
